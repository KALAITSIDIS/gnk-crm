/**
 * The line a timeline shows in place of a conversation note that erasure or
 * the retention sweep has blanked (0094, audit SEC-03). The event still says a
 * conversation happened, over which channel and when; only the words are gone.
 */
export const NOTE_ERASED_LABEL = "[note erased]";
